# Get this from https://dashboard.stripe.com/test/apikeys (sandbox) — never commit the real value
STRIPE_SECRET_KEY=sk_test_...

# Set after creating a webhook endpoint (Stripe CLI locally, or Dashboard for a deployed URL)
STRIPE_WEBHOOK_SECRET=whsec_...

# Where Checkout should send customers back to on success/cancel
APP_BASE_URL=http://localhost:3000

PORT=3000
